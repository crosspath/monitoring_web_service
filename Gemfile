source 'https://rubygems.org'

gem 'rails',         '4.1.9'
gem 'sass-rails', '~> 4.0.3'
gem 'uglifier', '>= 1.3.0' # do we need it?
# gem 'nodejs'

gem 'haml'
gem 'haml-rails'
gem 'sidekiq'
gem 'httparty'
gem 'bootstrap-sass'
gem 'redis'
gem 'redcarpet'

gem 'sanitize' # do we need it?

group :development do
    gem 'better_errors'
    gem 'binding_of_caller'
    gem 'spring'
end
